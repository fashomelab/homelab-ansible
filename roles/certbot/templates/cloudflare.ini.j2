# Cloudflare API credentials used by Certbot DNS plugin
# Placed in /etc/letsencrypt/cloudflare.ini with mode 0400
# Use the correct key name for API TOKEN authentication:
dns_cloudflare_api_token = {{ cf_api_token }}