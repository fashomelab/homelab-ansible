---
# This file ONLY defines physical hardware that cannot be auto-discovered.
all:
  children:
    # --- Physical hosts (Raspberry Pis) ---
    raspberry_pis:
      hosts:
        apollo.example.local:
        thunderjaw.example.local:
        sawtooth.example.local:
        ravager.example.local:
        stormbird.example.local:

    # --- Assign roles to specific Pis ---
    monitoring_server:
      hosts:
        stormbird.example.local: